# По условию не совсем понятно, что должно подаваться на ввод, а что на вывод, но допустим, что алгоритм следующий:
# Петя задумывает x и y (это будет ввод, сделаем через random), дальше мы считаем s и p и идем в обратную сторону - находим x и y из этих данных.
# Такой подход считаю логичным, потому что это будет математически корректнее, так как мы не будем задавать s и p случайным образом.
def fXY(s,p):
    a = []
    D = s**2-4*p
    if D<0:
        print("не могу решить")
    elif D == 0:
        a.append(s/2)
        a.append(s/2)
    else:
        a.append((s+D**0.5)/2)
        a.append((s-D**0.5)/2)
    return a
import random as r
x = r.randint(0,1000)
y = r.randint(0,1000)
print (x,y)
s = x+y
p = x*y
print (s,p)
a = fXY(s,p)
print("ответ: ", a)
